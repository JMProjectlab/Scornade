<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12 impose son propre écran de lancement : autant qu'il montre
         l'icône de l'app sur le même fond que l'écran de démarrage suivant,
         faute de quoi le lancement enchaîne deux images différentes. -->
    <style name="Theme.Scornade" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
    </style>
</resources>
